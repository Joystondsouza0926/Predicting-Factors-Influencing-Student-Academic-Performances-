[build.environment]
  PYTHON_VERSION = "3.11"

[build]
  command = "python -m pip install --upgrade pip setuptools wheel && python -m pip install -r requirements.txt && npm install && npm run build"
  publish = "build"
